<div class="container bd-dark">
    <div class="row">
        <div class="col-md-5 offsed-md-3">
            <div class="card card-body bg-dark p-4 text-white">

                <h1 class="text-white">Actualiza la visita</h1>
    <form action="/edit/{{task._id}}" method="post">

<input type="text" name="title" placeholder="Nombre" value="{{task.title}}" class="form-control rounded-0 bg-dark text-white" />
<textarea name="description" rows="3" placeholder="Fecha instalacion-Direccion-Observaciones" class="form-control rounded-0 bg-dark text-white" >{{task.description}}</textarea> 
<input type="text" name="celular" placeholder="celular" value="{{task.celular}}" class="form-control rounded-0 bg-dark text-white" />
<button class="btn btn-warning">Actualizar</button>


</form>
</div>
            
        </div>
    </div>
</div>